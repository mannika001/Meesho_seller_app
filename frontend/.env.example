# Only needed in production (Netlify) — the deployed backend's full API URL.
# Leave unset locally; Vite's dev proxy handles /api -> localhost:4000 instead.
VITE_API_BASE=https://your-service.onrender.com/api
